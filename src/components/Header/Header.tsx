import * as React from 'react'
import Logo from '../Logo'
import * as Styled from './styled'

const Header = () => {
  return (
    <Styled.Header>
      <Logo/>
    </Styled.Header>
  )
}

export default Header
